<UserControl x:Class="GamepadApp.Views.ControlsMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:localViews="clr-namespace:GamepadApp.Views"
             Background="Transparent">

    <Grid>

        <!-- GAMEPAD TESTER -->
        <localViews:GamepadTesterView x:Name="TesterPage"/>

        <!-- TUŞ ATAMALARI PENCERESİNİ AÇACAK BUTON -->
        <Button x:Name="OpenKeyBindingsBtn"
                Content="Tuş Atamaları"
                Width="160"
                Height="42"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Margin="0,0,30,25"
                Background="#252525"
                Foreground="White"
                BorderBrush="#3A3A3A"
                FontSize="14"
                Cursor="Hand"
                Panel.ZIndex="10"
                Click="OpenKeyBindingsBtn_Click"/>

    </Grid>
</UserControl>